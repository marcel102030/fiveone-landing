# Redirects de URLs antigas do quiz (preserva query string automaticamente)
/teste-dons    /descubra-seu-dom    301
/quiz          /descubra-seu-dom    301

# NOTA: NÃO usar `/*    /index.html    200` aqui.
# Esse fallback captura também os custom domains do mesmo Pages project
# (ex.: redeigrejanascasas.com) e os faz servir o index.html do Five One,
# quebrando o outro site. Como o app usa HashRouter (rotas via #/...),
# o fallback nem é necessário pro Five One — qualquer URL sem hash
# já carrega o index.html e a SPA roteia pelo fragmento.
